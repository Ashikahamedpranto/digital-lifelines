/// A single flag that tells the rest of the app whether it's running
/// as the full "Digital Lifelines" experience, or the simpler
/// "Where Have You Been All My Life?" party-game-only version.
class AppConfig {
  static bool isLiteVersion = false;
}
